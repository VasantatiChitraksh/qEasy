import 'package:flutter/material.dart';

class OutputDoctor extends StatelessWidget {
  const OutputDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
